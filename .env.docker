# Docker Environment Variables
WWWGROUP=1000
WWWUSER=1000

# Application
APP_PORT=80
APP_URL=http://localhost

# Database (for docker-compose.yml - local development)
FORWARD_DB_PORT=3306
DB_CONNECTION=mysql
DB_HOST=mysql
DB_PORT=3306
DB_DATABASE=anticerai
DB_USERNAME=sail
DB_PASSWORD=password

# Cache
CACHE_STORE=database
SESSION_DRIVER=database
QUEUE_CONNECTION=database

# Vite (for development)
VITE_PORT=5173